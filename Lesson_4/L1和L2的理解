1.L1正则易产生稀疏w的原因是在L1范数球是方形的，w更新过程中最优解易稳定在坐标轴上。
  而L2正则的范数球是圆形，随处都有可能得到最优解。

2.L1正则化效果是对原有最优解的每个元素产生不同量的偏移，并使某些元素为0，从而产生稀疏性。
  L2正则化的效果是对原有最优解的每个元素进行不同比例的放缩。

3.损失函数：
  L1范数损失函数：最小绝对值偏差（LAD），可以处理异常值
  L2范数损失函数：最小平方误差（LSE），会平方化误差，对异常值敏感

  区别：
  L2损失函数： L1损失函数：
  不太鲁棒     鲁棒（最小绝对值偏差之所以是鲁棒的，是因为它能处理数据中的异常值）
  稳定解       不稳定解
  总是一个解   可能很多解

4.正则化：
  L2正则化：               L1正则化：
  计算效率高（有解析解）   非稀疏情形下计算效率低
  非稀疏输出               稀疏输出
  无特征选择               有特征选择